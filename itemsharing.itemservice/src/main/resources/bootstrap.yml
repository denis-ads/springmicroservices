spring:
  application:
    name: itemservice
  profiles:
    active: default
  zipkin:
    baseUrl: http://localhost:9411
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
    
security:
  basic:
    enabled: false