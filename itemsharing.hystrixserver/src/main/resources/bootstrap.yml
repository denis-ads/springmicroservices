spring:
  application:
    name: hystrixserver
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest